//imports go here

module.exports = () => {
  const notify = [];
  //query our database, set result as data

  //munge data into an array of objects, users

  //hit coinbase and get btc price

  //map over users 
  
  //compare user.buy to btc price

  //if user.buy < btc price, push user to notify

  //if user.buy < btc price, delete from users where id = user.id

  return notify;
};
